import { create } from 'nodegui-stylesheet';

export const styles = create({
  task: {
    background: '#e3ffff',
    border: '1px solid',
    borderRadius: 10,
    paddingVertical: 10,
    paddingRight: 100,
    minHeight: 15,
  },
  taskText: {
    color: 'black',
  },
});
